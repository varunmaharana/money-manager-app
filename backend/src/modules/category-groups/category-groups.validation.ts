import { z } from "zod";

/*
export const createCategoryGroupsSchema =
	z.object({
		name: z
			.string()
			.trim()
			.min(1),
	});

export type CreateCategoryGroupsDto =
	z.infer<
		typeof createCategoryGroupsSchema
	>;

*/
