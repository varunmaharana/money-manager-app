import { z } from "zod";

/*
export const createAccountSchema =
	z.object({
		name: z
			.string()
			.trim()
			.min(1),
	});

export type CreateAccountDto =
	z.infer<
		typeof createAccountSchema
	>;

*/
