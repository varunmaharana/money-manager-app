import { z } from "zod";

/*
export const createAccountGroupSchema =
	z.object({
		name: z
			.string()
			.trim()
			.min(1),
	});

export type CreateAccountGroupDto =
	z.infer<
		typeof createAccountGroupSchema
	>;

*/
