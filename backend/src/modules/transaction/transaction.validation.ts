import { z } from "zod";

/*
export const createTransactionSchema =
	z.object({
		name: z
			.string()
			.trim()
			.min(1),
	});

export type CreateTransactionDto =
	z.infer<
		typeof createTransactionSchema
	>;

*/
