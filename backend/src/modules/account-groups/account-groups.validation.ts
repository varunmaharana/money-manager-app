import { z } from "zod";

/*
export const createAccountGroupsSchema =
	z.object({
		name: z
			.string()
			.trim()
			.min(1),
	});

export type CreateAccountGroupsDto =
	z.infer<
		typeof createAccountGroupsSchema
	>;

*/
